{#-
  Models barrel.

  Re-exports every generated model file plus the enums file so consumers can
  ``import 'package:<pkg>/models/models_index.dart';`` once. File names are
  passed in from Python; no entity names are hardcoded here.
-#}
// THIS CODE WAS INITIALLY GENERATED BY model-generator
// Barrel file re-exporting all generated models and enums.

export '{{ enums_file }}';
{% for export in exports %}
export '{{ export }}';
{% endfor %}
