{#-
  API barrel.

  Re-exports every generated retrofit client (and request DTO file) so consumers
  can ``import 'package:<pkg>/api/api_index.dart';`` once. File names are passed
  in from Python; no entity names are hardcoded here.
-#}
// THIS CODE WAS INITIALLY GENERATED BY model-generator
// Barrel file re-exporting all generated API clients.

{% for export in exports %}
export '{{ export }}';
{% endfor %}
