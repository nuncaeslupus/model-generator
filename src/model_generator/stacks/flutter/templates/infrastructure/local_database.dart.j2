{#-
  Drift AppDatabase template (Phase 4).

  Emits ``lib/core/local_database.dart`` — the single ``@DriftDatabase``
  class that aggregates every generated ``*Table`` class.  Regenerated on
  every model-gen run (like ``api_index.dart`` and ``models_index.dart``) so
  the table list stays complete across multi-file specs.

  IMPORTANT: ``schemaVersion`` starts at 1.  When you add a new entity (and
  thus a new table) or change an existing column, bump ``schemaVersion`` and
  add a ``MigrationStrategy`` here, then run:
    dart run build_runner build --delete-conflicting-outputs

  Variables injected by ``generate_drift_database``:
    tables   list of {class_name, import_uri} — sorted alphabetically.
-#}
// THIS CODE WAS INITIALLY GENERATED BY model-generator
// DO NOT EDIT — regenerated from the spec on every model-gen run.
//
// When the generated table list changes (new entity added, column altered),
// bump [schemaVersion] and add a [MigrationStrategy] to handle the upgrade.

import 'package:drift/drift.dart';
import 'package:drift_flutter/drift_flutter.dart';

{% for table in tables %}
import '{{ table.import_uri }}';
{% endfor %}

part 'local_database.g.dart';

/// Local SQLite database backing the offline-cache repositories.
///
/// Generated by model-generator — table list updated automatically when the
/// spec changes.  To activate offline caching, inject an instance into each
/// [*CachedRepository] and switch your DI from [*Repository] to
/// [*RepositoryCustom] (updating the constructor to accept this database).
@DriftDatabase(tables: [{{ tables | map(attribute='class_name') | join(', ') }}])
class AppDatabase extends _$AppDatabase {
  AppDatabase() : super(_openDatabase());

  @override
  int get schemaVersion => 1;

  static QueryExecutor _openDatabase() =>
      driftDatabase(name: 'app_database');
}
