{#-
  build.yaml — build_runner configuration for the freezed / json_serializable
  codegen. Generic defaults; project-agnostic.
-#}
# THIS CONFIG WAS INITIALLY GENERATED BY model-generator
targets:
  $default:
    builders:
      json_serializable:
        options:
          # Match the backend contract: explicit, non-nullable-aware decoding.
          explicit_to_json: true
          field_rename: none
          create_to_json: true
